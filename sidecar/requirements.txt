# --- Servidor ---
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
psutil==6.*

# --- Transcrição / IA ---
# Fase atual: faster-whisper (transcrição multilíngue robusta, timestamps por
# palavra). Diarização (pyannote/WhisperX) será adicionada numa fase posterior.
# Observação: faster-whisper usa CTranslate2; PyTorch entra com a diarização.
# Wheels costumam existir para Python 3.10–3.12; em 3.14 pode faltar suporte.
faster-whisper==1.*

# --- Análise de sentimento PT-BR ---
# pysentimiento traz modelos PT/EN/ES para sentimento e emoção (puxa
# torch/transformers — sem wheels para 3.14, daí a venv .venv312 em Python 3.12).
pysentimiento==0.7.*

# numpy é dependência transitiva de faster-whisper/torch; fixamos a major para
# evitar quebras. As análises quantitativa/qualitativa são em Python puro (sem
# pandas).
numpy==2.*

# --- Exportação ---
python-docx==1.*
reportlab==4.*
